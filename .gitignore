/build/
# 与仓库根 .gitignore 的 edr-agent/build-*/ 对齐；仅 clone 本目录时也不提交实验 CMake 输出
/build-*/
# vcpkg 清单模式在工程根生成的已安装包目录（CI 无此目录名冲突时可删）
/vcpkg_installed/
install/windows-inno/Output/
.onnx_gen_deps/
# 联调 ONNX 勿提交；models/ 目录内 README 等占位文件需入库（Inno 安装包带 models）
/models/*.onnx
.venv-proto/
/tmp
requirements-dev.txt
# 勿用 `/docs` 全目录 ignore：会阻止新的 Runbook（如 WP9）`git add`，且 CI 无法检出 → verify_ave_behavior_chain_invariants 失败。生成物可写具体路径，如 `/docs/.cache/` 等。
