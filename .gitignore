# Local CMake / experiment build outputs.
/build*/
/build_*/

# vcpkg manifest-mode install tree.
/vcpkg_installed/

# Windows installer output.
install/windows-inno/Output/
install/windows-setup-ui/bin/
install/windows-setup-ui/obj/
install/windows-setup-ui/Output/

# 取证采集器打包暂存(velociraptor.exe 等大体积/第三方件,由 fetch_velociraptor.sh 生成,不入库)。
install/windows-inno/collector_stage/

# Generated dependencies / virtualenvs / temporary workspaces.
/.onnx_gen_deps/
.venv*/
/tmp/
/INSTALL-PLATFORM/

# 联调 ONNX 勿提交；models/ 目录内 README 等占位文件需入库（Inno 安装包带 models）
/models/*.onnx
/models/*.ort

# Local assistant / IDE state.
/.trae/

# Generic local noise.
.DS_Store
Thumbs.db
*.log
*.tmp
*.bak
*.bak.*
/build-ir
/build-obs
/build_darwin
/out
/docs
