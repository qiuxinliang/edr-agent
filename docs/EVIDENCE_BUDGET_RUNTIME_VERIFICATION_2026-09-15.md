# 自动补证运行收敛验证

日期：2026-09-15。对应调研计划阶段 0，以及 D/E 中“保留并验证现有补证预算”。
本次修复部署与配置漂移，不新增补证限流器，不改规则评分或 LLM 输入。
以下时间均为 UTC，除明确注明的 OS 本地时间。

## 已验证的问题

上一轮 3.2.499 实测：启动后首个 30 分钟下发 50 条取证命令。
本轮 11:47:32 查询之前滚动 10 分钟仍有 29 条。

运行事实：

- API PID 34408，2026-09-15 19:26:35 +0800 启动，role=api。
- Worker PID 70875，2026-09-14 22:36:34 +0800 启动，role=worker。
- 预算修复提交 `3bfcb3c9` 在 2026-09-15 18:18:13 +0800，旧 Worker 未随之重启。
- 当前源码的预算查询面对实际已有命令会返回阻止条件成立；但旧运行进程继续下发。
- Worker 监督进程使用 `/private/tmp/edr-worker-supervisor.sh`，调用
  `/tmp/edr-worker-recovery.fd2xKD/start_worker.sh`，最终调用仓库现有启动 owner
  `edr-backend/scripts/restart_local_edr_api.sh fg`。
- 该本地启动包装脚本还固定了旧 public URL，覆盖项目配置中的新地址。

因此这轮没有另写一套“防重复”代码：先验证既有实现，再更新真正执行任务的 Worker。
下面的实际前后对照支持“修复未加载到执行 Worker”是此前重复下发的一个实际原因，
不宣称所有历史自动取证都由同一个原因触发。

## 实施与安全边界

1. 无数据库的手动/保护性动作及单次 claim 测试通过。
2. 初次 MySQL 测试调用被安全审查拦截，没有绕过；获得用户明确许可后，测试创建
   `edr_evidence_budget_test_*` 唯一临时数据库，执行后删除，不向 `edr` 业务库写测试数据。
3. 现有三项定向测试全部通过，包括真实 MySQL 下 8 个并发请求只放行一个。
   同时覆盖租户/终端边界、延迟旧告警、手动及保护性动作不受预算影响。
4. 服务端 `go build ./cmd/edr-api` 成功（输出到独立临时路径）。
5. 仅移除 Worker 本地包装脚本中的旧 public URL 覆盖。现有配置加载器读取
   `edr-backend/.env.edr-dev` 的 `https://192.168.43.86:8080`；不把该 IP 写进新增运行代码。
6. 确认 8081 监听、PID 和监督进程后，仅向旧 PID 70875 发 SIGTERM。
   原 `edr-worker-local` 监督机制调用既有 owner 重建并启动，不创建第二个 Worker。
7. 新 PID 45225，运行表 started_at=11:48:33.800，groups 与原 Worker 保持一致。
   API PID 34408、前端及自动隔离策略均未改变，未新增终端案例或手工补证命令。

实际启动日志中的工作区构建指纹：

- git=`829430c0(dirty)`；存在其他任务的预先修改，不是纯净提交构建。
- SHA256=`0298ec8e622e6be9e6b21fe7ddcefcdf3bf2e4f6a550f48f9d7f8359e8b13131`。
- mtime=`2026-09-15T19:48:29+0800`。

没有将这些其他任务的修改纳入本轮 Git 提交。运行参数包装脚本在本机临时目录，
本次已修正，但它不是通用生产部署包；不能据此宣称生产 API/Worker 版本一致性治理已完成。

## 实际验证数据

对象仅为 WIN-FAC3AC1PS5O；不将跨终端或跨时间窗总数相减作为丢失率。

| 固定窗口 | collect_forensic 新命令数 |
| --- | ---: |
| 更新前 11:46:33.800–11:48:33.800 | 2 |
| 更新后 11:48:33.800–11:50:33.800 | 0 |

同一个更新后两分钟窗口产生的决策：

- `blocked_evidence_budget`：8 条；未创建新的 automation_run_id。
- `blocked_by_gate`：4 条；与预算限制分开统计。
- Worker 持续心跳，说明没有通过停掉处理流程获得“零命令”。

可追踪预算决策示例：

- `TG-1C3FECFD1F15EEFA49160B3B`，11:49:04.222。
- `TG-0D422F2E83CA21A122B6874A`，11:49:14.028。
- `TG-95799DB844EDB82FDE0346E6`，11:50:01.934。

API `/healthz`=200、Worker `/healthz`=200，HTTPS 均正常验证证书，未使用跳过 TLS 校验选项。
前端 HTTP=200。独立测试库剩余数为 0；测试库已清理，未删除业务告警或证据。

这是短窗口的部署修复实证，不是同工作负载下的长期减量实验，不能宣布完成计划的
“重复任务降低 80%”目标。完整 10 分钟到期边界和长期运行仍需另行观察。

## 计划状态与下一步

- 已推进：既有预算并发行为、Worker 实际部署与地址配置、短窗口运行收敛。
- 未推进为完成：按需求关联已有任务或复用已完成证据；预算阻止不等于证据问题已解决。
- 未改：规则必要/可选/N/A、遥测/决策/处置拆分、LLM 包去重与引用、四组冻结样本回放。
- Agent 最新官方发布仍为 `win_3.2.499`；FileRead 修复 `94d41e8f` 尚无本轮查到的新版本产物，
  不把它说成已在 UTM 生效。
- 终端解除隔离未在本轮操作或重新验收；没有改变自动隔离策略。

下一次发布验收必须分别核实 API 和 Worker，而不只看磁盘二进制或 API 启动时间。
复用现有运行实例表与启动指纹，分别记录角色、PID、启动时间及所加载构建；
有工作区修改时明确 dirty 边界。只有涉及 Agent 的修复才安排 Windows 原生构建与签名发布。
