# WP-8：ETW/采集「compliance_baseline」— 全量可观测、与 P0 字段矩阵/会签对表时推荐
# 使用：在启动 edr_agent 的同一 shell 中 source（Linux）或 set（Windows cmd）
# 不替代 TOML：请保持 [collection] 各 Provider 与 agent.toml.example 同策略再关可选项；关 DNS/PS/Sec 前必查 Cauld Design/EDR_P0_Field_Matrix_Signoff.md
# 详：edr-agent/docs/WP8_ETW_COLLECTION_PROFILE.md

# 与 console heartbeat 同周期输出 ETW/总线观测行（见 edr/etw_observability_win.h）
set EDR_ETW_OBS=1

# 可选：同一观测行追加到文件（需应用有写权限）
# set EDR_ETW_OBS_EXPORT_PATH=C:\ProgramData\edr\etw_obs_baseline.log

# 常用：把心跳拉到 60s 与 ETW 观测行同读（可覆盖 [server].keepalive_interval_s 的默认心跳间隔）
# set EDR_CONSOLE_HEARTBEAT_SEC=60
